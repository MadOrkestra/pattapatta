---
"pattapatta": minor
---

Add `halftone` module: convert image tone (RGBA) to exclusive pen-plotter mark modes — angled lines or lattice circles (with optional overlapping circles for denser midtones). Stroke-only SVG via `toSvg`.
